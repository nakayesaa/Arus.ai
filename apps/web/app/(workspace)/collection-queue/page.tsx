import { PlaceholderPage } from '@/components/placeholder-page';

export default function CollectionQueuePage() {
  return (
    <PlaceholderPage
      eyebrow="Daily work"
      title="Collection Queue"
      description="Prioritized follow-up akan dihitung deterministically dari invoice, aging, promise, dispute, dan last contact."
    />
  );
}
