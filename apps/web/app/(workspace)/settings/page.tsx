import { PlaceholderPage } from '@/components/placeholder-page';

export default function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Owner only"
      title="Settings"
      description="Organization, timezone, members, roles, dan active state akan dikelola di sini."
    />
  );
}
