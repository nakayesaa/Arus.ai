import { PlaceholderPage } from '@/components/placeholder-page';

export default function ImportPage() {
  return (
    <PlaceholderPage
      eyebrow="Data intake"
      title="Import"
      description="Upload canonical CSV, review valid/invalid/duplicate rows, lalu commit valid rows dengan aman."
    />
  );
}
